from django.apps import AppConfig

class AuctionConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'auction'  # Assurez-vous que ce nom correspond au dossier de votre application

